(function () {
  // Ранний consent-гейт на уровне <head>.
  //
  // Исторически здесь до получения согласия на cookies задерживалась загрузка
  // сторонних скриптов, встроенных в страницы как inline-код сразу после <body>
  // (раньше футерного legal-consent.js). Сейчас таких скриптов нет, поэтому
  // блокировать нечего — гейт оставлен как совместимый no-op, чтобы не ломать
  // ссылки на /js/consent-gate.js в ранее собранных страницах и вызов
  // dppConsentGate.unblock() из legal-consent.js.

  window.dppConsentGate = {
    unblock: function () {},
  };
})();
